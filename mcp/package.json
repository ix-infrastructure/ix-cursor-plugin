{
  "name": "@ix/cursor-plugin-mcp",
  "version": "0.2.1",
  "description": "MCP server for ix-cursor-plugin",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "tsx --test tests/**/*.test.ts",
    "test:unit": "tsx --test tests/unit/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
